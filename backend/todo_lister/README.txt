Pasos para probar este codigo:
1) En el directorio del proyecto ejecutar un comando "composer install".
2) En MySQL cargar el archivo Dump proporcionado.
3) Para ejecutar el programa, se debera ejecutar el comando "symfony serve -d" en el directorio del proyecto